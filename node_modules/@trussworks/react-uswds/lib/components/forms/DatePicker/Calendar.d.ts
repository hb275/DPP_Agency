import React from 'react';
import { FocusMode } from './DatePicker';
import { DatePickerLocalization } from './i18n';
export declare const Calendar: {
    ({ date, selectedDate, handleSelectDate, minDate, maxDate, rangeDate, setStatuses, focusMode, i18n, }: {
        date?: Date | undefined;
        selectedDate?: Date | undefined;
        handleSelectDate: (value: string) => void;
        minDate: Date;
        maxDate?: Date | undefined;
        rangeDate?: Date | undefined;
        setStatuses: (statuses: string[]) => void;
        focusMode: FocusMode;
        i18n?: DatePickerLocalization | undefined;
    }): React.ReactElement;
    displayName: string;
};
